var editMarker = findMarker(<%= @activity.id %>);

openInfoWindow("<%= j(render :template => 'activities/edit.html.erb',
        :locals  => { :activity => @activity} ) %>", editMarker);

showHideUniqueField(editMarker.type);
